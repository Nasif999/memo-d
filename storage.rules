rules_version = '2';

// Same model as Firestore: clients never access Storage directly. Uploads and
// downloads go through Next.js API routes using the Admin SDK + signed URLs.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
